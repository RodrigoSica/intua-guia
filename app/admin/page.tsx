export const metadata = {
  title: "Admin | Intua Guia",
};

export default function AdminPage() {
  return (
    <main style={{ minHeight: "100vh", display: "grid", placeItems: "center", background: "#041b29", color: "#f9ecdc" }}>
      <p>Admin — protegido pelo Cloudflare Access.</p>
    </main>
  );
}
